import 'package:flutter/cupertino.dart';

class UserCallSupport extends StatelessWidget {
  const UserCallSupport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}